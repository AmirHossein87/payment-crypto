import { Skeleton } from './skeleton.directive';

describe('Skeleton', () => {
  it('should create an instance', () => {
    const directive = new Skeleton();
    expect(directive).toBeTruthy();
  });
});
